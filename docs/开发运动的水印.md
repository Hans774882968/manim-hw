给通义千问网页端的提示词：

大佬，你是一名数学科研工作者，精通manim。请叫我hans7。请写manim代码添加一个不断运动的文字作为水印，文案“Hans”。文字初始位置为右下角，有随机的初速度。运动到边界时按光的反射定律反射

生成效果基本OK，只是被其他图形挡住了。我就问通义千问怎么解决，它说用`self.bring_to_front()`。亲测没用，但我搜官方文档搜到`Mobject`有一个`z_index`方法。设置这个属性就解决了。
